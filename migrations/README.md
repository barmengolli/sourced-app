# Migrations

SQL migrations for the Sourced Supabase project. The canonical schema
lives in [`../SCHEMA.sql`](../SCHEMA.sql); files in this directory
record incremental changes applied on top of that baseline.

## Naming convention

```
YYYY-MM-DD_short_description.sql
```

Date is the day the migration was authored (not the day it was
applied). `short_description` is snake_case and should make the intent
obvious from the filename alone. Files sort alphabetically by date
prefix, which is also the intended apply order.

## Migrations in this directory

Listed in date order. Status reflects what has been applied to the
production Supabase project as of 2026-07-15. Statuses dated before
2026-05-06 have not been re-verified against prod since; confirm in the
Supabase dashboard before relying on them.

| File | Status | Notes |
|---|---|---|
| `2026-05-04_q1_date_correction_book_a_call.sql` | APPLIED | Moved 34 leads' `marketing_sourced_date` from 2026-04-02 to 2026-03-31 (Q1 boundary correction for the "Book a call" form) and locked the field. |
| `2026-05-04_q1_mql_history_correction_book_a_call.sql` | APPLIED | Of those 34 leads, moved 13 MQL `stage_history` entries' `entered_at` to 2026-03-31 so they bucket into Q1. |
| `2026-05-04_add_close_lost_stage.sql` | APPLIED | Relaxed the `stage_key` CHECK constraints on `attributions`, `funnel_projections`, and `funnel_actuals` to include `'closeLost'`. |
| `2026-05-07_attributions_unique_deal_stage.sql` | PENDING | F-006: Adds a partial UNIQUE index on `attributions (deal_id, stage_key)` (where `deal_id` is non-null and non-empty) to prevent duplicate downstream rows from any write path. Run the dedup query in the migration header before applying. |
| `2026-05-07_attributions_stage_entered_at.sql` | PENDING | Adds `stage_entered_at DATE NOT NULL` to `attributions` plus a supporting index. Backfills existing rows with `created_at::date`. Feeds the new Marketing Funnel: Velocity sub-tab. |
| `2026-05-12_backfill_missing_mql_stage_history.sql` | PENDING | One-time backfill: appends a `stage_history` MQL entry (entered_at = CURRENT_DATE) to every lead whose `current_stage` is `'mql'` but whose `stage_history` has no `'mql'` entry. Fixes the legacy MQL under-count from re-imports that updated `current_stage` without seeding history. Pair with the importer fix in `useLeads.ts` so future re-imports auto-append on stage upgrade. Preview-count snippet in the file header. |
| `2026-05-13_leads_event_activations.sql` | PENDING | Adds `event_activations TEXT[] NOT NULL DEFAULT '{}'` to `leads` plus a GIN index for membership queries. Feeds the new Marketing Funnel: Events sub-tab. No backfill needed: existing rows get `'{}'` from the column default; the next SFDC re-import populates real values from the "Event Activation" column. |
| `2026-05-14_campaign_costs.sql` | PENDING | Adds `campaign_costs` table (one row per date-range budget per channel) plus RLS policies, realtime publication, and an updated_at trigger. Feeds the new Marketing Funnel: Spend sub-tab. No data migration needed; users enter budgets via the Channel Manager UI after applying. |
| `2026-05-19_channels_year.sql` | APPLIED | Adds `year INTEGER` to `channels` plus a supporting index. Backfills from the existing `"YYYY -"` name prefix so every existing 2026 channel ends up `year=2026`; evergreen channels (no prefix) stay NULL. Drives the year-aware attribution-modal channel filter. |
| `2026-05-19_funnel_actuals_lead_mql.sql` | PENDING | Relaxes the `stage_key` CHECK on `funnel_actuals` to include `'lead'` and `'mql'`. Required so historical-year lead/MQL counts can be seeded as actuals when no lead-level data was imported (e.g. 2025 pre-Sourced). For years where real leads exist, lead/MQL counts continue to come from `leads.marketing_sourced_date` and `leads.stage_history`; funnel_actuals rows for these stages are fallback only. Pair with the compute.ts change on this branch. |
| `2026-05-19_seed_2025_channels_and_actuals.sql` | PENDING | Seeds the five 2025 top-level channels (`year=2025`, no sub-channels) plus 32 `funnel_actuals` rows (lead and MQL per quarter, four channels) and 32 `funnel_projections` rows (annual projections split evenly across quarters). Sales Generated has no Lead/MQL data but is created for completeness. Run AFTER `2026-05-19_funnel_actuals_lead_mql.sql`. |
| `2026-06-10_region_taxonomy_migration.sql` | PENDING | EIS region taxonomy: re-derives `leads.region` from country, auto-maps `attributions` LATAM→`EMEA cont & LATAM` and APAC→`UK&IRE, ME, Japan`, and produces a review list for old EMEA deals (manual split between the two new buckets). Run AFTER deploying the code change from `prompts/2026-06-10_eis-region-taxonomy.md`. Also update the n8n leads-sync region mapping the same day. Outreach tabs unaffected. |
| `2026-06-11_sixsense_snapshots.sql` | PENDING | Adds the `sixsense_snapshots` table (one row per 6sense "Activities By Source" summary import, keyed by `snapshot_date`) plus RLS policies and realtime publication. Idempotent (`IF NOT EXISTS`, guarded policies/publication). Feeds the new 6sense section (Dashboard + in-app CSV Import). No data migration; the MOps lead imports exports via the in-app importer after applying. An n8n + Google Sheet writer may target the same table later, tagged via the `source` column. |
| `2026-06-11_attribution_lost_reason.sql` | PENDING | Adds nullable `lost_reason TEXT` to `attributions`, captured by a required dropdown when a deal is marked close-lost ("Closed-Lost to Competitor" / "Closed-Lost In-House" / "Closed-Disqualified"). Plain TEXT (no CHECK) so the option list can evolve in the UI. No backfill: existing close-lost rows stay NULL ("No reason set") and are filled in manually via the deal editor. Idempotent (`ADD COLUMN IF NOT EXISTS`). |
| `2026-06-12_attribution_bdr_name.sql` | PENDING | Adds nullable `bdr_name TEXT` (+ index) to `attributions`: which BDR a deal is credited to, for the BDR Quota tracker. Set per deal in the editor (propagated across the deal chain like region). Plain TEXT matching the app's fixed roster ("Dave Cummins" / "Garrett McNally"). No backfill: deals are untagged until set. Idempotent (`ADD COLUMN IF NOT EXISTS`). |
| `2026-06-12_bdr_quotas.sql` | PENDING | Adds the `bdr_quotas` table (one row per bdr_name × year × stage_key in ('hpp','opp') holding the annual target `quota`) plus RLS + realtime. Idempotent (`IF NOT EXISTS`, guarded policies/publication). Feeds the new BDR Quota section: gauges measure computed actuals (Marketing-SDR first-touch deals matched by bdr_name) against these targets. Quotas entered in-app; no data migration. |
| `2026-06-16_sixsense_segment.sql` | PENDING | Adds a `segment TEXT NOT NULL DEFAULT 'Target Accounts in CRM'` to `sixsense_snapshots` so the 6Sense summary can be tracked per campaign (e.g. "Life & Annuities") alongside the overall report. Backfills existing rows to 'Target Accounts in CRM', then swaps the natural key from `UNIQUE(snapshot_date)` to `UNIQUE(snapshot_date, segment)` (drops the old constraint by column lookup, robust to its name) and adds a `(segment, snapshot_date DESC)` index. Idempotent. Segment is chosen from a dropdown on import; the dashboard stacks one section per segment, overall first. |
| `2026-06-18_sixsense_import_audit.sql` | PENDING | Adds nullable `file_name TEXT` + `imported_at TIMESTAMPTZ` to `sixsense_snapshots`, backfilling `imported_at = created_at` for existing rows. Powers the 6Sense Import-tab guardrails: an import-history registry (segment / month / imported-at / file) and a pre-confirm overwrite warning when the chosen (month, segment) already has data. The app sets `imported_at` on every upsert (insert and re-import); historical rows keep NULL `file_name`. Idempotent (`ADD COLUMN IF NOT EXISTS`). |
| `2026-07-06_campaign_tags.sql` | PENDING | Adds `campaign_tags` (canonical campaign: name, color, order) + `campaign_tag_links` (polymorphic 1:1 map from a tag to one asset via `(asset_type, asset_ref)` where asset_type ∈ channel/sixsense_segment/outreach_sequence and asset_ref is the channel UUID / segment string / sequence_id as text; `UNIQUE(asset_type, asset_ref)`). The manual tag layer that unifies the four data silos for the Campaigns tab: leads/opps join through tagged channels (existing FKs), 6Sense and Outreach are tagged directly. RLS (public read / anon write) + realtime, both guarded. Idempotent (`IF NOT EXISTS`, guarded policies/publication). |
| `2026-07-07_linkedin_ads.sql` | PENDING | Adds `linkedin_ads_snapshots` (one row per ad set × week: snapshot_date, year, week_number, campaign/product/region, adset_id=adset_name, spend/impressions/clicks). Weekly, n8n-fed from a Google Sheet; metrics are PER-WEEK (not cumulative), so the app sums rows directly. RLS + realtime, both guarded. Also widens `campaign_tag_links.asset_type` CHECK to add `'linkedin_adset'` (drops old CHECK by column lookup, re-adds). Idempotent. Apply after `2026-07-06_campaign_tags.sql`. |

| `2026-07-15_campaign_tag_links_multi_tag.sql` | APPLIED | Multi-tag assets: swaps `campaign_tag_links`' natural key from `UNIQUE(asset_type, asset_ref)` (1:1, tagging an asset to a second campaign MOVED it) to `UNIQUE(tag_id, asset_type, asset_ref)`, so one channel / segment / sequence / ad set can belong to several campaigns at once. Drops the old constraint by column lookup (robust to its name), collapses any duplicate rows first, and adds an `(asset_type, asset_ref)` index to replace the one the dropped constraint provided implicitly. Campaign totals INTENTIONALLY overlap once an asset is shared (a shared channel's leads and deals count in full for every campaign claiming it, so figures no longer sum to a company total); the Campaigns Overview says so in-UI. Apply after `2026-07-07_linkedin_ads.sql` and BEFORE deploying the paired code change on this branch (`useCampaignTags` / `CampaignTagPicker` / `campaignScorecard`): the new `linkAsset` upserts on the new key and errors against the old constraint. Idempotent. |
| `2026-07-24_opportunity_ledger_storage.sql` | APPLIED | Bite 5B: six new `sf_opportunity_*` tables for the Opportunity movement contract (snapshot, APPEND-ONLY history events with a UNIQUE Salesforce History ID and an UPDATE/DELETE-blocking trigger, 1:1-while-active deal links keyed only by exact Salesforce Opportunity ID, review-inbox PROJECTION with constrained issue codes, APPEND-ONLY review audit trail with actor/evidence/dedupe key and the same trigger, sync runs with SystemModstamp/history-CreatedDate watermarks). RLS ENABLED with deliberately NO policies (no anon access; service-role ingestion and a future authenticated review API are the writers); not in the anon-policy loop, not in realtime. Touches no existing table, row, policy, or attribution. Standalone, idempotent, forward-only. Applied manually to production on 2026-07-27. Verified through direct catalog inspection: all six sf_opportunity_* tables exist, RLS is enabled on all six, each has zero policies, and none is included in the supabase_realtime publication. |
| `2026-07-27_opportunity_ingestion_apply_fn.sql` | APPLIED | Bite 5C2A: adds review-evidence columns to `sf_opportunities` (normalized state, is_closed/is_won, customer-expansion raw, SDR/creator USER IDS, three Industry Vertical raws, GTM/LOB, SaaS revenue) and defines `sf_apply_opportunity_ingestion`: SECURITY DEFINER (search_path pinned to pg_catalog, schema-qualified references) applying one planned batch atomically against ONLY the six `sf_opportunity_*` tables. Run row created FIRST from server-generated values only (it survives malformed payloads; caller metadata is cast inside the protected block) and tagged onto every inserted event; CONCURRENCY-SAFE snapshot upsert (single INSERT ... ON CONFLICT DO UPDATE guarded to strictly-newer SystemModstamp; conflict-locked post-inspection classifies stale no-op, idempotent no-op, or same-timestamp conflict failure); event content verification (existing History Id with identical hash no-ops, different content FAILS the batch, never updated); review items carry ARRAYS of coupled audit events including audit_only conflict evidence (raced creates skip insert and all audits, zero-row issue updates fail, only issue_codes ever touched); audit dedupe compares the complete null-safe canonical identity (occurred_at excluded: first observation wins) and any difference fails; watermarks persist only on the completed row of a fully successful batch; failures roll back and record SANITIZED diagnostics (SQLSTATE + allowlisted category, never SQLERRM). Revoked from PUBLIC/anon/authenticated; EXECUTE only for service_role. Requires 2026-07-24_opportunity_ledger_storage.sql. Writes no data by itself. Applied manually to production on 2026-07-27. Verified through direct catalog inspection: all 13 expected evidence columns exist on public.sf_opportunities; public.sf_apply_opportunity_ingestion(jsonb,jsonb,jsonb,jsonb) exists, is SECURITY DEFINER, and has search_path=pg_catalog; anon cannot execute it, authenticated cannot execute it, PUBLIC execution is revoked, and service_role can execute it; RLS is enabled on all six sf_opportunity_* tables, all six have zero RLS policies, and none is included in the supabase_realtime publication. |
| `2026-07-29_lead_campaign_touches.sql` | APPLIED | Bite 4C of the lead multi-attribution program (docs/lead-multi-attribution-program.md): new `lead_campaign_touches` table storing one row per campaign membership touch, implementing the Bite 4A `LeadCampaignTouch` contract. Columns per the program data model (lead FK cascade, nullable SFDC CampaignMember Id / Campaign Id, nullable channel FK, touch_date, parent/sub campaign provenance, observed_at, source CHECK ('import','n8n_sync','backfill','manual'), raw JSONB audit). Indexes on lead_id, channel_id, touch_date; partial UNIQUE on campaign_member_id where non-null; natural-key partial UNIQUE (lead_id, campaign_id, COALESCE(touch_date, sentinel)) for Id-less rows, matching dedupeTouches. RLS enabled with the standard public-read + anon-write policies mirroring leads; added to the supabase_realtime publication (guarded). Seeds one 'backfill' touch per lead from (source_channel_id, marketing_sourced_date), guarded against re-run duplication, with a verification SELECT (seeded touches must equal leads with a source channel). Nothing reads the table yet: dashboard numbers are identical before and after. Rollback: DROP TABLE lead_campaign_touches is safe until Bite 4D/4E deploy. Standalone, idempotent. Applied manually to production on 2026-07-30; the seed verification SELECT returned touches = leads_with_channel = 2,862. |
| `2026-07-30_touch_seed_descendant_cleanup.sql` | APPLIED | Bite 4D.1 one-time cleanup (docs/lead-multi-attribution-program.md): the 4D import superseded backfill seeds only on an exact (lead, channel) match, leaving 244 parent-level seeds alive whose leads got child-level import touches (e.g. '2026 - Events' seed vs '2026 - Transformation Summit' touch); unfixed, 4E would double-count those leads within one channel family. Runs a recursive channel_family closure (UNION, cycle-safe) and, in order: pre-flight counts printed for comparison (expected surviving_seeds=751, descendant_superseded=244); a locked-date repair FIRST (leads with field_locks marketing_sourced_date=true whose seed is about to be superseded get their corrected date moved onto the superseding import touch, report date preserved in raw.sfdc_touch_date, only where dates differ); then deletes seeds with a same-lead identity-carrying import touch on the seed's channel or any descendant; verification prints surviving seeds (expected ~507), zero seeds with a same-family touch remaining, and zero leads left with no touches. Report-absent seeds (~507) intentionally untouched. Compare live pre-flight numbers against the expected values BEFORE COMMIT. Idempotent: a re-run repairs and deletes nothing. Applied manually to production on 2026-07-30; verification returned surviving_seeds=509, seeds_with_same_family_touch_remaining=0, leads_with_zero_touches=0. |
| `2026-08-04_lifecycle_observation_ledger.sql` | APPLIED | Bite 4G2A: seven new `sf_lifecycle_*` tables (sync runs with two independent page-completeness axes and watermarks, canonical persons, person aliases keyed on the exact Salesforce source record, APPEND-ONLY observations, APPEND-ONLY derived events, the mutable current projection, and a reviewable issues table) for the lifecycle observation ledger (docs/lead-lifecycle-observation-ledger.md). Exists because Bite 4G1 proved the org holds ZERO lifecycle field history on Lead and Contact and Salesforce never recreates it retroactively, so the past is unrecoverable and lifecycle truth must be accumulated forward. The FIRST observation of a person is a baseline that invents no Lead date, MQL transition, earlier stage, or requalification; observations are stored on material change with unchanged re-observations counted in the run row instead of inserting a row per person per night. Observations and events are enforced append-only by trigger (same mechanism as the 5B ledger); foreign keys use RESTRICT so nothing can delete audit history; normalized states, provenance, run status, and issue kinds are check-constrained; a CHECK enforces that a watermark can exist only on a completed run. Salesforce Ids live ONLY in the alias table as server-side evidence, unique per (source_object, source_record_id). RLS ENABLED on all seven with deliberately NO policies (no anon access); not in the anon-policy loop, not in realtime. Touches no existing table, row, or policy; writes no data; performs no backfill. Standalone, idempotent, forward-only. Applied manually to production on 2026-08-04; Supabase returned `Success. No rows returned.` Verified through direct catalog inspection: all seven `sf_lifecycle_*` tables exist, RLS is enabled on all seven, each has zero RLS policies, and none is included in the `supabase_realtime` publication; the append-only triggers exist on `sf_lifecycle_observations` and `sf_lifecycle_events`. Created structure only and imported no lifecycle data: no observations, events, persons, aliases, issues, or sync runs were inserted. Ingestion does not exist yet (Bite 4G2B is unstarted). |
| `2026-08-04_lifecycle_observation_apply_fn.sql` | APPLIED | Bite 4G2B1: adds three forward-only idempotency constraints the applied 4G2A schema lacked (`sf_lifecycle_event_key_unique`, `sf_lifecycle_issue_key_unique`, and `sf_lifecycle_observation_key_unique`, each with its backing key column) and defines `sf_apply_lifecycle_observations`: SECURITY DEFINER (search_path pinned to pg_catalog, every reference schema-qualified) applying ONE serialized batch atomically against ONLY the seven `sf_lifecycle_*` tables. The event constraint is not cosmetic: events had no unique key, so an exact retry would have inserted PERMANENT duplicates through the append-only trigger and inflated every downstream transition, return, and requalification count. Run row created FIRST from server-generated values so a malformed payload still records a failure; incomplete extraction on EITHER axis is refused before any state changes; temporary planner person handles are mapped to database UUIDs and never stored; aliases come only from an exact source record or an exact `ConvertedContactId` link, and a race or preexisting conflict that would merge two existing people FAILS the batch rather than choosing one; observations and events are insert-only with exact retries collapsing via their keys; same source timestamp with differing content is refused without selecting a winner; the projection is guarded by a strictly-newer comparison under a row lock so stale evidence cannot overwrite newer state; issues dedupe on evidence rather than run; the watermark persists ONLY on a fully successful batch, and a failure rolls back every business write leaving one failed run row with a NULL watermark and a SANITIZED summary (SQLSTATE plus an allowlisted category, never SQLERRM, which can embed source values). Revoked from PUBLIC/anon/authenticated; EXECUTE only for service_role. Requires 2026-08-04_lifecycle_observation_ledger.sql. Writes no data by itself; imports nothing; backfills nothing. Applied manually to production on 2026-08-05. Verified through direct catalog inspection: all seven `sf_lifecycle_*` tables exist with RLS enabled, zero policies, and no `supabase_realtime` publication membership; all three idempotency key columns (`observation_key`, `event_key`, `issue_key`) and their unique constraints exist; `sf_apply_lifecycle_observations` (seven JSONB arguments) and `sf_lifecycle_resolve_person` (two JSONB arguments) both exist, are owned by postgres, are SECURITY DEFINER with `search_path=pg_catalog`, and carry the ACL `postgres=X/postgres | service_role=X/postgres`, so neither PUBLIC, anon, nor authenticated can execute them while service_role can; both append-only triggers on observations and events exist and are enabled. All seven lifecycle tables contained zero rows, so the migration created structure only and imported no lifecycle data. Ingestion does not exist yet: Bite 4G2B2 remains unstarted and inactive. |
| `2026-08-11_sfdc_campaign_member_daily_apply.sql` | APPLIED | Installs `sourced_apply_sfdc_campaign_members(jsonb)`, the atomic server-side apply boundary for the disabled daily Salesforce CampaignMember workflow. One reconciled batch upserts edit-lock-aware people, creates or resolves the parent and child channel hierarchy, and upserts one `lead_campaign_touches` row per CampaignMember ID. Every eligible membership remains a Lead; an MQL observation also preserves one MQL history entry, so a fast same-day Lead-to-MQL still counts as Lead and MQL in the acquisition cohort. Exact retries update the same lead and touch instead of duplicating them; exact 15/18-character Salesforce identity equivalence is supported; conflicting identities fail closed; real memberships supersede same-family backfill seeds. SECURITY DEFINER with `search_path=pg_catalog`; revoked from PUBLIC/anon/authenticated and granted only to service_role. Creates no business data by itself. Applied manually to production on 2026-08-11 after the generated workflow's aggregate-only dry run reconciled 2,630 source memberships into 2,614 eligible memberships and 16 missing-email exclusions, with zero writes. Supabase returned `Success. No rows returned.` Direct catalog inspection verified owner `postgres`, SECURITY DEFINER, `search_path=pg_catalog`, ACL `postgres=X/postgres | service_role=X/postgres`, no EXECUTE for PUBLIC/anon/authenticated, and EXECUTE for service_role. The separate first controlled invocation then processed all 2,614 eligible memberships, inserted 2,614 child-campaign touches, excluded the same 16 missing-email rows, produced zero duplicate CampaignMember IDs and zero parent-level touch assignments, and reconciled 539 current-MQL plus 135 historical-only MQL memberships into 674 cohort MQL memberships. |
| `2026-08-11_sfdc_campaign_touch_import_supersession.sql` | APPLIED | Repairs the duplicate counting exposed by the first controlled Salesforce daily apply. A production read-only audit found 2,608 legacy `source='import'` touches with no CampaignMember ID shadowed by an authoritative `n8n_sync` touch for the same canonical person and exact child channel (Content Syndication Q3 displayed 150 Leads / 82 MQLs instead of 77 / 43). The one-time DELETE is constrained to those proven shadows and leaves n8n_sync, manual, backfill, unmatched import, lead, and channel rows untouched. Adds a restricted AFTER INSERT/UPDATE trigger applying the same predicate to future authoritative memberships so a legacy import cannot coexist with its n8n replacement. Applied manually to production on 2026-08-11. Direct aggregate-only verification returned zero remaining shadows, 2,614 authoritative n8n touches, one intentionally unmatched legacy import, and `prevention_trigger_exists=true`. Idempotent; requires the daily-apply migration above. |
| `2026-08-12_opportunity_daily_ingestion_contract.sql` | APPLIED | Adds the confirmed editable Salesforce Market source column, a check-constrained BDR suggestion (`Dave Cummins` / `Garrett McNally`) used only as review evidence, plus reviewer-owned overrides for Market, Commercial Region, and GTM Cube; source-channel override continues to use the existing review `channel_id`. Defines restricted service-role-only `sf_apply_opportunity_ingestion_v2` and `sf_read_opportunity_ingestion_state` RPCs. Applied manually to production on 2026-08-12; Supabase returned `Success. No rows returned`. Direct catalog verification confirmed both RPCs are `SECURITY DEFINER` with `search_path=pg_catalog`, executable by `service_role`, and denied to `PUBLIC`, `anon`, and `authenticated`. Structure/functions only; no Opportunity data was imported or backfilled by this migration. |
| `2026-08-12_opportunity_reporting_projection.sql` | APPLIED | Adds `source_system` and exact `sf_opportunity_id` provenance to `attributions`, plus a generated Opportunity-stage uniqueness guard. Applied manually to production on 2026-08-12. Direct catalog verification confirmed both check constraints, the exact-ID index, and the Salesforce Opportunity/stage partial unique index. Existing rows remain `manual`; no review was approved, deal linked, or attribution created. |
| `2026-08-12_funnel_account_identity_and_lifecycle_provenance.sql` | APPLIED | Adds exact Salesforce Account identity to `leads`, `attributions`, and protected Opportunity staging so MQL-account-to-HPP conversion never joins on editable names. Adds versioned service-role-only CampaignMember v2 and Opportunity v3 apply wrappers around the already-proven production boundaries. Applied manually to production on 2026-08-12. Direct catalog verification confirmed all expected columns and indexes; both functions are owned by `postgres`, use `SECURITY DEFINER` with `search_path=pg_catalog`, deny PUBLIC/anon/authenticated, and grant EXECUTE only to `service_role`. The controlled baseline correction labelled 26 initial Salesforce MQL observations as `baseline`, leaving zero unclassified. Includes no source read, reporting-row promotion, or automatic attribution approval. |
| `2026-08-12_campaign_member_v2_set_based_hardening.sql` | PENDING | Replaces only the CampaignMember v2 wrapper after its first 2,618-membership invocation exceeded Supabase's statement timeout. The proven v1 atomic apply remains unchanged; Account/prestate work moves from per-membership queries to a transaction-local grouped input and one set-based Lead update. Preserves exact Account identity conflict refusal, baseline-versus-transition provenance, service-role-only execution, and full transactional rollback. Does not import or alter business data by itself. |


Status legend: **APPLIED** (run against prod), **PENDING** (committed
but not yet run), **UNKNOWN** (provenance unclear, verify in Supabase
before applying).

## Applying pending migrations from a fresh checkout

There is no migration runner wired into the app. Two options:

1. **Supabase SQL Editor (current practice).** Open the project in the
   Supabase dashboard, navigate to SQL Editor, paste the file
   contents, run. Apply files in alphabetical order. Each migration is
   wrapped in its own transaction; if it fails, fix and rerun.
2. **Supabase CLI (if adopted later).** Move files into
   `supabase/migrations/` (matching the CLI's expected layout) and run
   `supabase db push`. The project does not currently use the CLI; this
   is documented for future use only.

For a brand-new database, run [`../SCHEMA.sql`](../SCHEMA.sql) first,
then apply any migrations dated after the schema's "as of" header in
order. SCHEMA.sql is kept current with applied migrations folded in,
so on a clean install most files in this directory will be redundant
but harmless to re-run (each is idempotent or guarded).

## Adding a new migration

1. Create `YYYY-MM-DD_description.sql` in this directory.
2. Write the migration. Prefer idempotent statements (`IF NOT EXISTS`,
   `DROP ... IF EXISTS` before `CREATE`, `ON CONFLICT DO NOTHING` for
   seeds) so re-running is safe.
3. If the migration changes structure (new table, new column, changed
   CHECK constraint), update [`../SCHEMA.sql`](../SCHEMA.sql) in the
   same commit so the canonical schema stays current.
4. Apply it in Supabase, then update the table above with status
   APPLIED.
